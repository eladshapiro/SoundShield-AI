# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
# pyaudio>=0.2.11  # Optional: only needed for live microphone input (requires portaudio19-dev system package)

# IMPORTANT: FFmpeg is required for Whisper transcription
# Install FFmpeg before using Whisper features:
#   Windows: choco install ffmpeg (or download from https://ffmpeg.org/download.html)
#   Linux: sudo apt-get install ffmpeg
#   macOS: brew install ffmpeg
# See INSTALL_FFMPEG.md for detailed instructions

# Machine Learning & AI (using compatible versions)
tensorflow>=2.16.0
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.30.0
scikit-learn>=1.3.0

# Audio Analysis
pyAudioAnalysis>=0.3.10
webrtcvad>=2.0.10

# Serialization (must be >=1.0.0 for librosa compatibility)
msgpack>=1.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0
flask-limiter>=3.5.0  # API rate limiting
PyJWT>=2.8.0  # JWT authentication
bcrypt>=4.1.0  # Password hashing
flask-socketio>=5.3.0  # WebSocket support for live monitoring

# Utilities
python-dotenv>=1.0.0
python-json-logger>=2.0.0  # Structured JSON logging
tqdm>=4.65.0
Pillow>=10.0.0
fpdf2>=2.7.0  # PDF report generation

# Optional Hebrew language support (if available)
# hebpipe>=1.0.0

# Advanced Models (Primary - used by default)
openai-whisper>=20231117  # Fallback transcription backend (REQUIRES FFmpeg - see INSTALL_FFMPEG.md)
faster-whisper>=1.1.0     # Primary transcription backend (CTranslate2); per-language models, GPU float16
# CTranslate2 links against CUDA 12 while recent PyTorch wheels ship CUDA 13. These two wheels
# provide the CUDA 12 runtime libraries that cuda_compat.py pre-loads (Linux/Windows with NVIDIA GPU).
nvidia-cublas-cu12; platform_system != "Darwin"
nvidia-cudnn-cu12>=9,<10; platform_system != "Darwin"
# AudioSet event tagger (audio_tagger.py) and dimensional emotion model (emotion_models.py) are
# plain `transformers` models: MIT/ast-finetuned-audioset-10-10-0.4593 and
# audeering/wav2vec2-large-robust-12-ft-emotion-msp-dim (downloaded on first use, ~1.6 GB total).
# transformers>=4.30.0  # Already listed above
pyannote.audio>=3.1.0  # For speaker diarization
# torch>=2.0.0  # Already listed above
